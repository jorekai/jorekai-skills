# Decisions

One file per decision about the collection itself, numbered, never edited after the fact: a superseding decision gets a new file that names the old one. Format: Context, Decision, Consequences, each a short paragraph. The rules in `STYLE.md` and `AGENTS.md` say what; the files here say why, so that nobody "fixes" a rule without knowing the reason.
